name: apply-reference-select-migration

on:
  push:
    branches: [feature/reference-data-all-selects]

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Migration runner prepared"
